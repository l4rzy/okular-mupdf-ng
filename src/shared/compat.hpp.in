#ifndef MUPDF_SHARED_COMPAT_HPP
#define MUPDF_SHARED_COMPAT_HPP

#include <string_view>

namespace Mu::IPC {

// Compatibility version
inline constexpr std::string_view COMPAT = "@MU_COMPAT_VERSION@";

} // namespace Mu::IPC

#endif // MUPDF_SHARED_COMPAT_HPP
